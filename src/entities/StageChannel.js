const Channel = require('./Channel');

module.exports = class StageChannel extends Channel {
	constructor(channel, guild, gocheLibrary) {
		super(channel, guild, gocheLibrary);
	}

    request() {

    }


    leave() {
        
    }
};
